%h1 Edit new resume section to #{@resume.name}.
= form_for @resume_section, url: edit_resume_sections_path(@resume) do |f|
  = render 'form', f: f

= render 'form'

= link_to 'Show', @resume_section
\|
= link_to 'Back', resume_sections_path
